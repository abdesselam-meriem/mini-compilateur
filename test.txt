BeginProject Test;

Setup :
define a | b | c: integer;
%%commentair zyada brk
define b : integer = 10;
define moyenne : float = 0.0;
const Pi : float = 3.14159;
define Tab : [integer ;20] ;

Run :

a ← 5 + (-3) * 2;
if (a+b > c) then:
{
    out("a+b est plus grand");
}else{
    out("c est plus grand");
}
endif;

loop while (c<10)
{ c ← c + 1;
} endloop;
output(b);
output("good");
EndProject;